// previously committed chunk - doesn't comply with prettier rules
const foo = "bar";

if (foo === 'bar') {
  console.log('foo!');
}

// added chunk - doesn't comply with prettier rules
console.log("foobar!");
